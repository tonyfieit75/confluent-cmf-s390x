apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cmf-sqlite-pvc
  namespace: confluent-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: managed-nfs-storage    # adjust to your OCP class

